# Friendly-Terminal-Using-Shell
A natural-language shell wrapper you never have to leave: type plain English and get things done, create/move/delete files and folders, search by name or by content, open files by description, navigate with a real GUI-style split-screen dashboard (tmux), and drop into any raw shell (bash/zsh) command whenever you want.
